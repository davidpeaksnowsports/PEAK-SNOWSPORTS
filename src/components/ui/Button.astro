---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'ghost';
  class?: string;
}

const { href, variant = 'primary', class: className = '', ...rest } = Astro.props;

const base = 'inline-flex items-center justify-center gap-2 rounded-full px-6 py-3 text-sm font-semibold uppercase tracking-wider no-underline transition';
const variants = {
  primary: 'bg-yellow text-olive-deep hover:bg-salt',
  secondary: 'border border-olive/40 text-olive hover:border-yellow hover:bg-yellow',
  ghost: 'text-olive hover:bg-yellow',
};

const cls = `${base} ${variants[variant]} ${className}`;
---

{href ? (
  <a href={href} class={cls} {...rest}><slot /></a>
) : (
  <button class={cls} {...rest}><slot /></button>
)}
