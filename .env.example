# Sanity
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
PUBLIC_SANITY_API_VERSION=2024-10-01
SANITY_READ_TOKEN=

# Resend (transactional email)
RESEND_API_KEY=

# Formspree (contact form)
PUBLIC_FORMSPREE_ENDPOINT=

# Plausible (cookieless analytics — no consent banner required)
PUBLIC_PLAUSIBLE_DOMAIN=peaksnowsports.com

# Retargeting pixels — consent-gated (load only after the visitor accepts the
# cookie banner). Leave blank to disable both the pixels AND the banner.
PUBLIC_META_PIXEL_ID=
PUBLIC_GOOGLE_ADS_ID=

# Weglot
PUBLIC_WEGLOT_API_KEY=

# SkiOperator booking embed — publishable-key mode.
# Optional overrides. The component (src/components/booking/SkiOperatorEmbed.astro)
# has the Peak Snowsports tenant + pub_ key hardcoded as defaults — pub_ keys
# are browser-safe per the SkiOperator docs, security is enforced by the
# origin allowlist in SkiOperator admin → Settings → Embed. Only set these
# if you need to point a build at a different tenant/key without editing
# the component (multi-tenant future, key rotation without code change, etc).
PUBLIC_SKI_OPERATOR_TENANT=
PUBLIC_SKI_OPERATOR_PUBLIC_KEY=

# SkiOperator booking embed — secure server mode (/lessons/private only).
# The sec_ key is SERVER-ONLY: no PUBLIC_ prefix, never commit it, never put it
# in browser code. Set it in Vercel → Settings → Environment Variables for
# Preview + Production, and in your local .env. /lessons/private mints a fresh
# embed token from it on every request (src/components/booking/
# SkiOperatorSecureEmbed.astro). Without it that page falls back to the
# "get in touch" message.
SKI_OPERATOR_ALL_PRODUCTS=

# Each SkiOperator embed has its own sec_ key, and the key is what selects which
# embed renders, so every page with its own embed needs its own key here and in
# Vercel (Preview + Production). A missing key is not fatal: that page falls back
# to the "get in touch" message instead of a broken widget.
SKI_OPERATOR_PRIVATE_LESSONS=
SKI_OPERATOR_GROUP_LESSONS=
SKI_OPERATOR_KIDS_GROUPS=
SKI_OPERATORS_LOCAL_KIDS_CLUB=
SKI_OPERATOR_OFF_PISTE=
SKI_OPERATOR_RACE_COACHING=
SKI_OPERATOR_SKI_CAMPS=

# --- Instructor portal ------------------------------------------------------
# Server-only. None of these carry a PUBLIC_ prefix: the portal is entirely
# server-rendered and no credential reaches the browser.

# Supabase project holding instructor accounts and portal records.
# The anon key is safe to hold server-side; row-level security is what
# protects the data. Never put the service-role key here.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# PRIVATE Sanity dataset for portal documents — NOT `production`, whose reads
# are public. Policies, pay rates and the code of conduct live here.
SANITY_PORTAL_DATASET=portal
SANITY_PORTAL_TOKEN=
